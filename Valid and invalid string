%{
#include <stdio.h>
%}

%%

^[AEIOUaeiou][a-zA-Z]*    { printf("String starting with vowel: %s\n", yytext); }
.|\n                      { /* Ignore other inputs */ }

%%

int main()
{
    yylex();
    return 0;
}

int yywrap()
{
    return 1;
}
