%{
#include <stdio.h>
#include <string.h>

#define OLD_WORD "oldword"
#define NEW_WORD "newword"
%}

%%

{OLD_WORD}    { printf("%s", NEW_WORD); }
.|\n          { printf("%s", yytext); }

%%

int main()
{
    yylex();
    return 0;
}

int yywrap()
{
    return 1;
}
